# 🖊️ Handwritten Digit Recognition App

This is a simple web application built with **Streamlit** that uses a trained deep learning model to recognize handwritten digits (0–9). The model is trained on the MNIST dataset and deployed using Streamlit Cloud.

## 🚀 Features
- Upload an image of a handwritten digit.
- The app preprocesses the image and predicts the digit using a CNN model.
- Displays the predicted digit and confidence score.
- Clean and user-friendly interface.

## 🛠️ Tech Stack
- Python
- TensorFlow / Keras
- Streamlit
- NumPy
- Pillow

## 📂 Project Structure